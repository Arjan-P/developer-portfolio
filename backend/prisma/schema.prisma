generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Pages {
  id             Int    @id @default(autoincrement())
  serialNumber   Int
  title          String
  content        String
}
